import styled from "@emotion/styled";

export const Screen = styled.div`
  position: absolute;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  background: linear-gradient(to top, #0008 0%, transparent 25%);
`;

export default Screen;
